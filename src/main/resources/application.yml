server:
  port: 443
  servlet:
    context-path: /mall
  ssl:
    enabled: true
    key-store: server.jks
    key-store-password: Houmaolong1985
    key-store-type: JKS
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall?serverTimezone=GMT%2b8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    database: MYSQL
    hibernate:
      ddl-auto: update
    